import { ButtonMyLocation, MapView, ReactLogo, SearchBar } from "../components"

export const HomePage = () => {
    return (
        <>
            <MapView />
            <ButtonMyLocation />
            <ReactLogo />
            <SearchBar />
        </>
    )
}